experiment:
  name: research_2026_05_22_gin_cora_confirm
  seeds: [0, 1, 2]
  write_research_results: true
  research_results_path: research_results.tsv

models:
  - gin

datasets:
  - cora

training:
  max_epochs: 200
  patience: 50
  scheduler: none

sweep:
  model.hidden_channels: [128]
  model.num_layers: [3]
  model.dropout: [0.5]
  model.eps_trainable: [true]
  training.lr: [0.01]
