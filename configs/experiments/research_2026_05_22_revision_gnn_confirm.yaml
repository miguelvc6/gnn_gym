experiment:
  name: research_2026_05_22_revision_gnn_confirm
  seeds: [0, 1, 2]
  write_research_results: true
  research_results_path: research_results.tsv

models:
  - revision_gnn

datasets:
  - cora
  - pubmed

training:
  max_epochs: 200
  patience: 50
  scheduler: none

sweep:
  model.hidden_channels: [128]
  model.num_layers: [2]
  model.dropout: [0.5]
  training.lr: [0.01]
  training.weight_decay: [0.0005]
