experiment:
  name: research_2026_05_22_gcn_batch1
  seeds: [0]
  write_research_results: true
  research_results_path: research_results.tsv

models:
  - gcn

datasets:
  - cora
  - pubmed

training:
  max_epochs: 50
  patience: 15
  scheduler: none

sweep:
  model.hidden_channels: [64, 128]
  model.num_layers: [2, 3]
  model.dropout: [0.2]
  training.lr: [0.01, 0.005]
