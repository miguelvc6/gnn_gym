experiment:
  name: research_2026_05_22_gat_batch1
  seeds: [0]
  write_research_results: true
  research_results_path: research_results.tsv

models:
  - gat

datasets:
  - cora
  - pubmed

training:
  max_epochs: 50
  patience: 15
  scheduler: none

sweep:
  model.hidden_channels: [16, 32]
  model.heads: [4]
  model.dropout: [0.2]
  model.attention_dropout: [0.0, 0.2]
  training.lr: [0.01, 0.005]
